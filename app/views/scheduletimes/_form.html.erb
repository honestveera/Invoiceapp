<%= form_for(@scheduletime) do |f| %>
  <% if @scheduletime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scheduletime.errors.count, "error") %> prohibited this scheduletime from being saved:</h2>

      <ul>
      <% @scheduletime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scheduletime %><br>
    <%= f.time_select :scheduletime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
